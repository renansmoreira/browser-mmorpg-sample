{
  "name": "dunno-client",
  "version": "0.0.1",
  "main": "config.js",
  "scripts": {
    "start": "npx browser-sync start --server --files \"*.html\" --port 3300",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx browser-sync start --server --files ./dist/*.js,*.html",
    "build:watch": "npx webpack -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "ts-loader": "^8.0.5",
    "typescript": "^4.0.3",
    "webpack": "^5.1.0",
    "webpack-cli": "^4.0.0"
  },
  "dependencies": {
    "socket.io-client": "^2.3.1"
  },
  "description": ""
}
